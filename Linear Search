int linearSearch(int n, int num, vector<int> &arr)
{
    int m;
    for(int i=0;i<n;i++)
    {
        if(num==arr[i])
        {
            return i;
        }
        else
        {
           m=-1;
        }
    }
    return m;
}
