#include <bits/stdc++.h> 
vector<int> rotateArray(vector<int>& arr, int n) 
{
 vector <int> ans;
 int k=1;
 for(int i=k;i<n;i++)
 {
    ans.push_back(arr[i]);
 }    
 for(int i=0;i<1;i++)
 {
     ans.push_back(arr[i]);
 }
 return ans;
}
