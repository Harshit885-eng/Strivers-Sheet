int isSorted(int n, vector<int> a) {
    int m;
    for(int j=1;j<n;j++)
    {
        if(a[j]>=a[j-1])
        {
            m=1;
        }
        else{
            return 0;
        }
    }
    return m;
}
