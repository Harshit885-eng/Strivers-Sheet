vector<int> moveZeros(int n, vector<int> a) {
    vector<int> ans;
    for(int i=0;i<n;i++)
    {
        if(a[i]!=0)
        {
            ans.push_back(a[i]);
        }
    }
    for(int i=0;i<n;i++)
    {
        if(a[i]==0)
        {
            ans.push_back(a[i]);
        }
    }
    return ans;
}
